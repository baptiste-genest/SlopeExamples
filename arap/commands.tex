% slope uses a project's commands.tex as the latex preamble, and re-renders the
% formulas when this file is saved.
\usepackage{libertine}
\usepackage{libertinust1math}
\usepackage{xcolor}

% the colours the shaders draw with, so the labels can name them
\definecolor{rest}{HTML}{7B8BA3}
\definecolor{moved}{HTML}{E2725B}
\definecolor{fitted}{HTML}{D9A441}
\definecolor{pinned}{HTML}{2A3340}
